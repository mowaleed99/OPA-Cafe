const features = ['Dashboard', 'POS', 'Tables', 'Products', 'Categories', 'Suppliers', 'Purchases', 'Closing', 'Users', 'Settings'];

features.forEach(f => {
  // We'll create placeholders individually below
});
